-- ??????

on run agrv
    set currentDate to current date

    -- ???????
    set yesterdayDate to currentDate - days

    -- ????
    display dialog "?????: " & currentDate & return & "?????: " & yesterdayDate
end run